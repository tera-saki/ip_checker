{
  "scripts": {
    "start": "forever start --id ip_checker -a -o out.log -e err.log src/main.js",
    "stop": "forever stop ip_checker"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "forever": "^3.0.2"
  }
}
